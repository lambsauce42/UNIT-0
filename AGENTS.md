At the beginning after a new user prompt I need you to consisely and precisely state how you view the user instruction and what open assumptions you are going to take if the user prompt wasnt specific. But only on implementation prompts not on generic ones.

Fallbacks are unacceptable. Do not add alternate visual paths, backup behavior, duplicate implementations, or masking logic to hide a broken primary system. If something requires a fallback, the underlying architecture is wrong: identify and fix the root cause instead.
